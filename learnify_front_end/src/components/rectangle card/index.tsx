import { Card,CardTitle,CardSubTitle } from "./styles"
import { Text } from "react-native"

export default function RectangleCard() {
    return (
        <Card>
            <CardTitle>Classic Nowels</CardTitle>
            <CardSubTitle>Promotion</CardSubTitle>
        </Card>
    )
}